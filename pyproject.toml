[project]
name = "url-shortener"
version = "0.1.0"
description = ""
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastapi>=0.141.1",
]

[dependency-groups]
dev = [
    "pre-commit>=4.6.2",
]
